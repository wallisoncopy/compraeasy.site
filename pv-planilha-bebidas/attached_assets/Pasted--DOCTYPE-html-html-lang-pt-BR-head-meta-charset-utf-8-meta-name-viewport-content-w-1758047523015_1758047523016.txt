<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<title>Planilha Inteligente de Bebidas - Controle, Evite Perdas e Lucre Mais</title>
<meta name="description" content="Planilha Inteligente de Bebidas para bares e distribuidoras — controle estoque, alertas e sugestão de reposição. Pague no PIX e receba agora.">
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap" rel="stylesheet">
<style>
  :root{
    --bg1:#000;
    --bg2:#0f0f12;
    --accent:#f1c40f;
    --muted:#bdbdbd;
    --card:#111217;
    --green:#22c55e;
  }
  html,body{height:100%;margin:0;font-family:Inter,system-ui,Arial,sans-serif;background:linear-gradient(180deg,var(--bg1),var(--bg2));color:#fff}
  .wrap{max-width:1100px;margin:24px auto;padding:20px}
  .topbar{display:flex;justify-content:space-between;align-items:center;gap:12px}
  .countdown{background:rgba(255,255,255,0.03);padding:10px 14px;border-radius:10px;font-weight:600;display:flex;gap:12px;align-items:center}
  .countdown small{color:var(--muted);font-weight:400}
  .hero{display:grid;grid-template-columns:1fr 420px;gap:28px;align-items:center;margin-top:22px}
  .hero-left h1{font-size:34px;margin:0 0 12px;color:var(--accent)}
  .hero-left h2{font-size:20px;margin:0 0 14px;color:#eee}
  .hero-left p{color:var(--muted);line-height:1.5}
  .mockups{display:flex;flex-direction:column;gap:12px;align-items:center}
  .mockup-img{width:100%;max-width:420px;border-radius:10px;box-shadow:0 10px 30px rgba(0,0,0,0.6);border:1px solid rgba(255,255,255,0.02)}
  /* Benefits */
  .benefits{display:grid;grid-template-columns:repeat(2,1fr);gap:12px;margin-top:22px}
  .benefit{background:var(--card);padding:14px;border-radius:10px;border:1px solid rgba(255,255,255,0.03)}
  .benefit h3{margin:0;color:var(--accent);font-size:15px}
  .cta-row{display:flex;gap:12px;align-items:center;margin-top:18px;flex-wrap:wrap}
  .price-badge{background:var(--accent);color:#000;padding:14px 20px;border-radius:10px;font-weight:700}
  .btn-pix{background:var(--green);color:#021; padding:14px 18px;border-radius:10px;border:none;font-weight:700;cursor:pointer}
  .btn-secondary{background:transparent;border:1px solid rgba(255,255,255,0.08);padding:12px 14px;border-radius:10px;color:var(--muted);cursor:pointer}
  /* Features columns */
  .features{display:grid;grid-template-columns:1fr 1fr;gap:12px;margin-top:22px}
  .feature-card{background:rgba(255,255,255,0.02);padding:12px;border-radius:8px}
  .feature-card h4{margin:4px 0;color:var(--accent)}
  /* Testimonials */
  .testimonials{display:flex;gap:10px;overflow:auto;margin-top:20px;padding-bottom:6px}
  .testimonial{min-width:250px;background:var(--card);padding:12px;border-radius:8px;border:1px solid rgba(255,255,255,0.03)}
  .plans{display:flex;gap:12px;margin-top:20px;flex-wrap:wrap}
  .plan{flex:1;min-width:260px;background:linear-gradient(180deg,rgba(255,255,255,0.02),rgba(255,255,255,0.01));padding:16px;border-radius:10px;border:1px solid rgba(255,255,255,0.03)}
  .plan .price{font-size:22px;font-weight:800;color:var(--accent)}
  .guarantee{margin-top:10px;color:var(--muted);font-size:14px}
  .faq{margin-top:18px;background:transparent;padding:12px;border-radius:8px}
  footer{margin-top:26px;padding:20px;color:var(--muted);text-align:center}
  /* responsive */
  @media(max-width:980px){
    .hero{grid-template-columns:1fr}
    .mockups{order:-1}
    .benefits{grid-template-columns:1fr}
    .features{grid-template-columns:1fr}
  }
  /* modal */
  .modal-backdrop{position:fixed;inset:0;background:rgba(0,0,0,0.6);display:none;align-items:center;justify-content:center;z-index:999}
  .modal{background:#0f0f12;padding:18px;border-radius:10px;max-width:520px;width:100%;border:1px solid rgba(255,255,255,0.04)}
  .modal h3{margin-top:0;color:var(--accent)}
  .pix-box{background:#050505;padding:12px;border-radius:8px;margin-top:8px;border:1px dashed rgba(255,255,255,0.05)}
  .copy-btn{margin-top:10px;padding:10px 12px;border-radius:8px;background:var(--accent);border:none;cursor:pointer;font-weight:700}
  .close-btn{background:transparent;border:1px solid rgba(255,255,255,0.06);color:var(--muted);padding:8px 10px;border-radius:8px;cursor:pointer;margin-left:8px}
</style>
</head>
<body>
  <div class="wrap">
    <!-- Top / Countdown -->
    <div class="topbar">
      <div class="countdown">
        <small>🔥 A PRÓXIMA OFERTA TERMINA EM:</small>
        <div id="countdown" style="font-weight:800">00:14:49</div>
      </div>
      <div style="color:var(--muted);font-size:14px">Planilha Inteligente • Bares & Distribuidoras</div>
    </div>

    <!-- Hero -->
    <section class="hero">
      <div class="hero-left">
        <h1>📊 Planilha Inteligente de Bebidas</h1>
        <h2>Controle seu estoque de bebidas, evite perdas e aumente o lucro</h2>
        <p>Ferramenta direta no navegador — ideal para distribuidoras, bares e empórios. Calcule ponto de reposição, receba alertas automáticos, gere lista de compras e veja o lucro potencial por produto.</p>

        <div class="cta-row">
          <div class="price-badge">✔️ Oferta por apenas <strong>R$ 17</strong></div>
          <button class="btn-pix" onclick="abrirPix('basic')">💳 Pague no PIX e receba seu acesso agora mesmo</button>
          <button class="btn-secondary" onclick="abrirPix('premium')">🚀 Quero o Premium R$27</button>
        </div>

        <div class="benefits" style="margin-top:16px">
          <div class="benefit">
            <h3>Controle visual do estoque</h3>
            <p style="color:var(--muted);margin:6px 0 0">Gráficos e alertas que mostram o que está prestes a faltar.</p>
          </div>
          <div class="benefit">
            <h3>Alertas automáticos de reposição</h3>
            <p style="color:var(--muted);margin:6px 0 0">Receba sinal quando o estoque atingir o ponto ideal de compra.</p>
          </div>
        </div>

        <div class="features">
          <div class="feature-card">
            <h4>✔ Sugestão exata de compra</h4>
            <p style="color:var(--muted);margin:6px 0 0">Com base no consumo semanal e estoque atual.</p>
          </div>
          <div class="feature-card">
            <h4>✔ Cálculo de lucro automático</h4>
            <p style="color:var(--muted);margin:6px 0 0">Veja o ganho por unidade e lucro potencial mensal.</p>
          </div>
          <div class="feature-card">
            <h4>✔ Banco inicial de produtos</h4>
            <p style="color:var(--muted);margin:6px 0 0">Comece rápido com itens já prontos — edite preços em segundos.</p>
          </div>
          <div class="feature-card">
            <h4>✔ Salve direto no navegador</h4>
            <p style="color:var(--muted);margin:6px 0 0">LocalStorage: dados salvos por usuário, sem precisar servidor.</p>
          </div>
        </div>
      </div>

      <!-- Mockups -->
      <div class="mockups">
        <!-- Substitua os src pelas suas imagens (upload no Replit) -->
        <img src="mockup_laptop.png" alt="Mockup laptop" class="mockup-img" />
        <img src="mockup_phone.png" alt="Mockup celular" class="mockup-img" style="max-width:260px" />
      </div>
    </section>

    <!-- O que leva / benefícios -->
    <section style="margin-top:20px">
      <h3 style="color:var(--accent);margin-bottom:8px">O que você recebe agora</h3>
      <div style="display:grid;grid-template-columns:repeat(3,1fr);gap:12px">
        <div class="feature-card"><strong>✔ Controle do estoque</strong><div style="color:var(--muted);margin-top:6px">Consumo médio, estoque mínimo e alertas.</div></div>
        <div class="feature-card"><strong>✔ Relatórios</strong><div style="color:var(--muted);margin-top:6px">Export CSV / PDF com lista pronta para enviar ao fornecedor.</div></div>
        <div class="feature-card"><strong>✔ Dashboard</strong><div style="color:var(--muted);margin-top:6px">Gráficos de giro, produtos que mais vendem e encalham.</div></div>
      </div>
    </section>

    <!-- Testimonials -->
    <section style="margin-top:20px">
      <h3 style="color:var(--accent)">O que donos de bares e distribuidoras estão dizendo</h3>
      <div class="testimonials">
        <div class="testimonial"><strong>⭐⭐⭐⭐⭐</strong><p style="color:var(--muted)">“Reduzi falta de estoque nos finais de semana e aumentei vendas. Ferramenta simples e prática.”</p><small style="color:var(--muted)">— João, Distribuidora Mix</small></div>
        <div class="testimonial"><strong>⭐⭐⭐⭐⭐</strong><p style="color:var(--muted)">“Agora sei exatamente quanto comprar antes do evento. Evitei prejuízo em grandes datas.”</p><small style="color:var(--muted)">— Carla, Bar & Co.</small></div>
        <div class="testimonial"><strong>⭐⭐⭐⭐⭐</strong><p style="color:var(--muted)">“Rápido, salvou horas de conferência manual. Salvei minha lista por navegador.”</p><small style="color:var(--muted)">— Pedro, Empório Central</small></div>
      </div>
    </section>

    <!-- Planos -->
    <section style="margin-top:22px">
      <h3 style="color:var(--accent)">Escolha seu plano</h3>
      <div class="plans">
        <div class="plan">
          <h4>Plano Básico</h4>
          <div class="price">R$ <span class="price">17</span></div>
          <p style="color:var(--muted)">Acesso rápido • Produtos limitados • Salve no navegador</p>
          <button class="btn-pix" style="margin-top:10px" onclick="abrirPix('basic')">Pague no PIX R$17</button>
          <div class="guarantee">Garantia de 30 dias</div>
        </div>

        <div class="plan">
          <div style="display:flex;justify-content:space-between;align-items:center">
            <h4>Plano Premium (Mais vendido)</h4>
            <div style="background:var(--accent);padding:6px 10px;border-radius:6px;color:#000;font-weight:700">R$27</div>
          </div>
          <p style="color:var(--muted)">Acesso vitalício • Produtos ilimitados • Dashboard completo • Suporte WhatsApp</p>
          <button class="btn-pix" style="margin-top:10px" onclick="abrirPix('premium')">Pague no PIX R$27</button>
          <div class="guarantee">Garantia de 30 dias</div>
        </div>
      </div>
    </section>

    <!-- FAQ -->
    <section class="faq" aria-labelledby="faq-title" style="margin-top:18px">
      <h3 style="color:var(--accent)">Perguntas Frequentes</h3>
      <div style="color:var(--muted);margin-top:8px">
        <p><strong>Como recebo o acesso?</strong> Após o pagamento no PIX, envie o comprovante no WhatsApp (número aparece no modal) e você receberá o link de acesso em minutos.</p>
        <p><strong>Funciona no celular?</strong> Sim — app web responsivo, funciona em celular, tablet e desktop.</p>
        <p><strong>Tem garantia?</strong> Sim — 30 dias de garantia total. Se não gostar, devolvemos seu dinheiro.</p>
      </div>
    </section>

    <footer>
      © 2025 Planilha Inteligente • Todos os direitos reservados
    </footer>
  </div>

  <!-- Modal PIX -->
  <div id="modalBackdrop" class="modal-backdrop" onclick="fecharModal(event)">
    <div class="modal" onclick="event.stopPropagation()">
      <h3 id="modalTitle">Pague no PIX e receba seu acesso</h3>
      <p style="color:var(--muted)">Escolha a forma: copie a chave PIX ou gere QR (no app do banco). Depois envie o comprovante para o WhatsApp para liberação imediata.</p>

      <div class="pix-box">
        <strong>Chave PIX:</strong>
        <div id="pixKey" style="margin-top:6px;color:#fff;font-weight:700">seu-pix-chave@exemplo.com</div>
        <button class="copy-btn" onclick="copiarPix()">Copiar chave PIX</button>
        <button class="close-btn" onclick="fecharModal()">Fechar</button>
      </div>

      <div style="margin-top:12px;color:var(--muted);font-size:13px">
        <strong>WhatsApp para envio do comprovante:</strong> <br><span style="color:var(--accent);font-weight:700">+55 11 9XXXX-XXXX</span>
      </div>
    </div>
  </div>

<script>
  // ===== CONTAGEM REGRESSIVA (14min50s) =====
  (function countdownInit(){
    // tempo em segundos (por exemplo 14min50s = 14*60 + 50)
    let total = 14*60 + 50;
    const el = document.getElementById('countdown');
    function tick(){
      if(total<=0){ el.textContent = "00:00:00"; return; }
      const m = Math.floor(total/60), s = total%60;
      el.textContent = String(m).padStart(2,'0') + ":" + String(s).padStart(2,'0');
      total--;
      setTimeout(tick,1000);
    }
    tick();
  })();

  // ===== PIX / MODAL =====
  const pixKey = "seu-pix-chave@exemplo.com"; // <-- substituir pela sua chave PIX
  const afterPaymentUrl = ""; // opcional: URL de confirmação/obrigado
  function abrirPix(plan){
    document.getElementById('modalBackdrop').style.display='flex';
    document.getElementById('pixKey').textContent = pixKey;
    document.getElementById('modalTitle').textContent = plan === 'premium' ? "PIX - Plano Premium R$27" : "PIX - Plano Básico R$17";
  }
  function fecharModal(e){
    if(e && e.target !== document.getElementById('modalBackdrop')) return;
    document.getElementById('modalBackdrop').style.display='none';
  }
  function copiarPix(){
    const key = pixKey;
    navigator.clipboard.writeText(key).then(()=> {
      alert("Chave PIX copiada! Agora cole no app do banco e converta em QR/pagamento.");
      // opcional: redirecionar para página afterPaymentUrl
      // if(afterPaymentUrl) window.location.href = afterPaymentUrl;
    }).catch(()=> alert("Não foi possível copiar automaticamente. Selecione a chave e copie manualmente."));
  }

  // fechar modal com ESC
  document.addEventListener('keydown', e=> { if(e.key==='Escape') fecharModal(); });

  // permitir clicar nos botões secundários para abrir modal
  // (já atribuídos no HTML)
</script>
</body>
</html>
